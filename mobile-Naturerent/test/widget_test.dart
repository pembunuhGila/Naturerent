// Basic Flutter widget test placeholder for NatureRent.
import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('NatureRent app smoke test placeholder',
      (WidgetTester tester) async {
    // Supabase requires async initialization — skip full pump in unit tests.
    // Run integration tests via `flutter test integration_test/` instead.
    expect(true, isTrue);
  });
}
